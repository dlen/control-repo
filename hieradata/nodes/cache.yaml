---
wordpress::backends:
  backend1: '192.168.0.1'
  backend2: '192.168.0.2'
  backend3: '192.168.0.3'

varnish::vcl::functions:
  vcl_recv: |
		if (req.url ~ "wp-admin|wp-login") {
			return (pass);
		}
	vcl_fetch: |
    if (!(req.url ~ "wp-(login|admin)")) {
    	unset beresp.http.set-cookie;
    }
